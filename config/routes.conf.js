// Import all individual routes
const divisionRoutes = require('../routes/Division.routes');
const positionRoutes = require('../routes/Position.routes');

// export all routes
module.exports = { divisionRoutes, positionRoutes };
